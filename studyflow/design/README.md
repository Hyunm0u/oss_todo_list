# design (참고용)

이 폴더는 빌드/실행에 포함되지 않는 **디자인 참고 자료**입니다.

- `stitch/` : Stitch가 출력한 8개 화면의 원본 HTML 목업.
  각 화면의 풍부한 인터랙션(시간 휠 피커, 바텀시트, 모달, 아코디언 등)이
  그대로 들어 있어, React 페이지를 고도화할 때 동작 레퍼런스로 사용합니다.
- `prototype/StudyFlow.html` : 위 8개 화면을 iframe으로 묶어 하단 탭으로
  전환하도록 만든 단일 파일 프로토타입. 브라우저로 바로 열어 볼 수 있습니다.
